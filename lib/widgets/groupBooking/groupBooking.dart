import 'package:flutter/material.dart';

class GroupBooking extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SingleChildScrollView(
      child: Center(child: Text('Group Booking')),
    );
  }
}
